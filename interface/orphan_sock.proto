syntax = "proto3";

package orphan_sock;

service OrphanSockServer {
  // rpc SayHello (HelloRequest) returns (HelloReply);
  rpc RegisterCloseProcess (CloseRequest)  returns (Empty);
}

message Empty {
}

message CloseRequest {
  int64 process_id = 1;
}
